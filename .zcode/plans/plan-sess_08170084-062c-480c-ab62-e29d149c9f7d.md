I completely understand. Mass search-and-replace scripts can definitely be risky and hard to track. Since you're in plan mode and I cannot execute any commands, here is the exact code you need to copy and paste to implement this manually.

Before you start, make sure to install `next-themes`:
```bash
npm install next-themes
```

### 1. `app/globals.css`
Update your globals to include the Tailwind v4 custom dark variant and remove the hardcoded background/text colors in the `body` block so Tailwind classes can take over.

```css
/* frontend-admin/app/globals.css */
@import "tailwindcss";
@custom-variant dark (&:is(.dark *));

@theme {
  --font-sans: 'Vazirmatn', sans-serif;
}

:root {
  --foreground-rgb: 0, 0, 0;
  --background-start-rgb: 243, 244, 246;
  --background-end-rgb: 255, 255, 255;
}

body {
  direction: rtl; /* تنظیم جهت کل اپلیکیشن به راست به چپ */
  font-family: 'Vazirmatn', sans-serif;
  /* background-color: #f3f4f6; Removed: handled by layout.tsx Tailwind classes */
  /* color: #1e293b; Removed: handled by layout.tsx Tailwind classes */
}

/* سفارشی‌سازی تقویم امتداد */
.rmdp-ep-arrow {
  background-color: #1a2e44 !important;
}
.rmdp-wrapper {
  border-radius: 1.5rem !important;
  box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1) !important;
  border: none !important;
}
.rmdp-header-values {
  color: #1a2e44 !important;
  font-weight: bold !important;
}
.rmdp-day.rmdp-selected span {
  background-color: #c5a059 !important; /* رنگ طلایی برای روز انتخاب شده */
  box-shadow: none !important;
}
.rmdp-day:not(.rmdp-disabled):not(.rmdp-day-hidden) span:hover {
  background-color: #1a2e44 !important;
  color: white !important;
}
```

### 2. `app/components/ThemeProvider.tsx`
Create this new file to expose the `next-themes` provider wrapped in `"use client"`.

```tsx
// frontend-admin/app/components/ThemeProvider.tsx
"use client";

import { ThemeProvider as NextThemesProvider } from "next-themes";

export function ThemeProvider({
  children,
  ...props
}: React.ComponentProps<typeof NextThemesProvider>) {
  return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
}
```

### 3. `app/components/ThemeToggle.tsx`
Create this new component for the actual toggle button. It uses a `mounted` state to prevent React hydration errors when reading the system theme on the first render.

```tsx
// frontend-admin/app/components/ThemeToggle.tsx
"use client";

import { useTheme } from "next-themes";
import { useEffect, useState } from "react";
import { Moon, Sun } from "lucide-react";

export function ThemeToggle() {
  const [mounted, setMounted] = useState(false);
  const { theme, setTheme } = useTheme();

  useEffect(() => {
    setMounted(true);
  }, []);

  if (!mounted) {
    return <div className="w-9 h-9" />; // Placeholder to avoid layout shift
  }

  return (
    <button
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
      className="p-2 rounded-xl bg-white dark:bg-[#182234] border border-gray-200 dark:border-slate-800 text-[#1a2e44] dark:text-slate-100 hover:bg-gray-50 dark:hover:bg-slate-800 transition-colors"
      aria-label="Toggle Dark Mode"
    >
      {theme === "dark" ? <Sun size={18} /> : <Moon size={18} />}
    </button>
  );
}
```

### 4. `app/layout.tsx`
Update your root layout to wrap everything in the `ThemeProvider`. Notice the addition of `suppressHydrationWarning` on the `<html>` tag, which is required by `next-themes`.

```tsx
// frontend-admin/app/layout.tsx
import './globals.css';
import { ThemeProvider } from './components/ThemeProvider';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="fa" dir="rtl" suppressHydrationWarning>
      <head>
        {/* فونت وزیرمتن */}
        <link href="https://cdn.jsdelivr.net/gh/rastikerdar/vazirmatn@v33.003/Vazirmatn-font-face.css" rel="stylesheet" />
      </head>
      <body className="bg-[#faf9f6] text-[#1a2e44] dark:bg-[#0b0f19] dark:text-slate-100 transition-colors duration-200">
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
```

### 5. `app/admin/dashboard/page.tsx`
Here is a snippet of the Dashboard page showing how to insert the `<ThemeToggle />` into the header and apply the dark mode classes (`dark:bg-[#182234]`, `dark:text-slate-100`, etc.) to the layout wrapper and the header elements.

```tsx
// frontend-admin/app/admin/dashboard/page.tsx
'use client';
import React, { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { ThemeToggle } from '@/app/components/ThemeToggle'; // <-- 1. Import
import api from '@/app/lib/api';
import {
  LogOut, Plus, Users, Trophy, Globe, TrendingUp
} from 'lucide-react';
import { ResponsiveContainer, AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip } from 'recharts';

export default function AdminDashboard() {
  const router = useRouter();
  const [stats, setStats] = useState({ users: 0, contests: 0, topProvince: 'در حال بارگذاری...', growth: 0 });
  
  // ... (Keep your existing data fetching and logic) ...

  return (
    {/* 2. Remove hardcoded bg from here, as we moved it to layout.tsx */}
    <div className="min-h-screen font-sans pb-10" dir="rtl">
      
      {/* Header */}
      <header className="p-8 flex justify-between items-center">
        <div>
          {/* 3. Add dark:text-slate-100 to headings */}
          <h1 className="text-3xl font-black tracking-tight text-[#1a2e44] dark:text-slate-100">پنل مدیریت امتداد</h1>
          {/* 4. Add dark:text-slate-400 to muted text */}
          <p className="text-gray-400 dark:text-slate-400 text-sm font-bold mt-1">مانیتورینگ هوشمند سیستم</p>
        </div>

        <div className="flex items-center gap-3">
          {/* 5. Insert Theme Toggle */}
          <ThemeToggle />
          
          {/* 6. Add dark:bg-[#182234] to secondary buttons */}
          <button 
            onClick={() => router.push('/admin/add-question')}
            className="bg-white dark:bg-[#182234] text-[#1a2e44] dark:text-slate-100 border border-gray-200 dark:border-slate-800 px-6 py-3 rounded-2xl font-black flex items-center gap-2 shadow-sm active:scale-95 transition-all hover:bg-gray-50"
          >
            <Plus size={20} className="text-[#c5a059]" /> 
            <span>افزودن سوال</span>
          </button>

          {/* Primary buttons usually stay the same in dark mode, or you can tweak them slightly */}
          <button
            onClick={() => router.push('/admin/create-contest')}
            className="bg-[#1a2e44] text-white px-6 py-3 rounded-2xl font-black flex items-center gap-2 shadow-xl shadow-blue-900/20 active:scale-95 transition-all hover:bg-[#2a405a]"
          >
            <Plus size={20} className="text-[#c5a059]" /> 
            <span>مسابقه جدید</span>
          </button>
        </div>
      </header>

      <main className="px-8 space-y-8">
         {/* ... Rest of your dashboard ... */}
      </main>
    </div>
  );
}

// Ensure you also update subcomponents like StatCard:
function StatCard({ title, value, icon, color, onClick }: any) {
  const displayValue = typeof value === 'number' ? value.toLocaleString() : value;
  return (
    <div
      onClick={onClick}
      // Add dark:bg-[#182234] and dark:border-slate-800 to Cards
      className={`bg-white dark:bg-[#182234] p-6 rounded-[2rem] shadow-sm border border-gray-100 dark:border-slate-800 flex items-center gap-5 transition-transform hover:-translate-y-1 ${onClick ? 'cursor-pointer active:scale-95 select-none' : ''}`}
    >
      <div className={`p-4 rounded-2xl ${color === 'blue' ? 'bg-blue-50 text-blue-500' : color === 'gold' ? 'bg-orange-50 text-orange-500' : 'bg-emerald-50 text-emerald-500'}`}>
        {React.cloneElement(icon, { size: 28 })}
      </div>
      <div>
        <p className="text-[10px] font-black text-gray-400 dark:text-slate-400 uppercase tracking-widest">{title}</p>
        <p className="text-2xl font-black text-[#1a2e44] dark:text-slate-100 mt-1">{displayValue}</p>
      </div>
    </div>
  );
}
```

Once you apply these changes, you can use the same `dark:bg-[#182234]`, `dark:text-slate-100`, and `dark:border-slate-800` utility patterns across the rest of the pages (like the Participants table) to complete the dark mode implementation!